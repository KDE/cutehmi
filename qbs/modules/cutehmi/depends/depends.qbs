import qbs

Module {
	//<workaround id="qbs.cutehmi.depends-1" target="Qbs" cause="design">
	//	Property 'reqMinors' is used instead of 'reqMinor' dependency parameter, which makes this module useless.
	//Parameter { property int reqMinor }
	//</workaround>
}
