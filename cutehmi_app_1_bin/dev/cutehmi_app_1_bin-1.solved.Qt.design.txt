Problem:

ApplicationWindow does not show up when visibility is set to Window.AutomaticVisibility.

Workaround:

Set Window.Maximized as default window visibility value.

Snippet:

Solution:

Window shows up if visible parameter is additionaly set to true.